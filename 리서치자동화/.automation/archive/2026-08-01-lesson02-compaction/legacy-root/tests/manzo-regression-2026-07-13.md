# 만조리서치 회귀 시험 — 실제 데이터 1건 (2026-07-13, 기가레인 049080)

`01-input/manzo-real-2026-07-13-049080.txt`(Gemini 분석 전 원본)와 실제 커밋된
`stock-analysis-data.json`의 같은 항목(출력)을 `02-reference/manzo-output-contract.md` 기준으로
대조한 기록입니다. 목 입력이 아니라 이미 게시된 실제 결과를 사후 검토한 것입니다.

## 대조 결과

| 체크 항목 | 결과 | 근거 |
| --- | --- | --- |
| 분량 | 통과 | riseReason 실측 약 320자 이상(200자 기준 충족), chartAnalysis 4문단 600자 이상 충족 |
| 뉴스 근거 대부분 일치 | 통과 | "한국항공우주연구원과 27억 규모 시제품 개발 계약", "K-오픈랜"·"일본 진출" 등은 실제 기사 제목 2·4·5와 일치 |
| **사실관계 — 불일치 발견** | **실패** | riseReason에 "한국거래소로부터 '현저한 시황변동에 대한 조회공시 요구'를 받았다"는 문장이 있으나, 수집된 기사 5건 제목 어디에도 조회공시 관련 내용이 없음. 뉴스에 없는 사실이 추가된 것으로 보임 |
| **수치 일치 — 불일치 발견** | **실패** | chartAnalysis가 "5일 이동평균선이 20일 이동평균선을 강하게 상향 돌파하는 골든크로스를 형성"이라고 서술하지만, 같은 항목의 실제 `technicals.cross`는 `null`(크로스 없음으로 계산됨). ma5(9636)>ma20(7690)이라 5일선이 위에 있는 건 맞지만 "돌파(크로스)"가 실제로 있었는지는 코드가 `null`로 판단함 — Gemini는 이 수치를 프롬프트로 받지 못해 그럴듯하게 추정해 쓴 것으로 보임 |
| 뉴스 summary 필드 | 확인 필요(코드 문제) | 기사 5건 모두 `summary`가 빈 문자열. `fetch_article_summary()` 추출 실패로 보이며, 제목만으로 분석이 이뤄짐 |

## 판단

이 1건만으로도 `02-reference/manzo-output-contract.md`의 "사실관계"·"수치 일치" 체크 항목이 실제로
걸러야 할 사례가 있다는 게 확인됨. 원인은 입력 설계 자체에 있음: `analyze_stock()`의 Gemini 프롬프트에
`technicals` 실제 수치가 전달되지 않아서, 차트 분석 문장이 실제 계산값과 다르게 나올 수 있음.

**게시 판단**: 이미 게시된 건이라 되돌리지 않음(이 문서는 사후 검토 기록). 다음 실행부터 재발을
막으려면 아래 "확인 필요"를 사람이 먼저 결정해야 함.

## 확인 필요 (사람이 결정)

- `analyze_stock()` 프롬프트에 실제 `technicals` 수치를 함께 넣어 chartAnalysis가 그 수치를 근거로
  쓰도록 코드를 고칠지
- riseReason에 "기사에서 확인된 사실만" 지시가 있음에도 사실무근 문장이 나온 사례를 더 모아,
  Gemini 프롬프트에 "기사에 없는 사실은 쓰지 말라"는 문구를 더 강하게 넣을지
- `fetch_article_summary()`가 계속 빈 문자열만 반환하는 원인(사이트 구조 변경 등)을 언제 조사할지
